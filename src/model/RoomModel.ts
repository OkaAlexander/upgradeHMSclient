interface RoomModel {
  roomNumber: string;
  roomCapacity: number;
  roomStatus: string;
  roomGender: string;
  roomSize: number;
  keyStatus: string;
  Id: string;
  hostelId: string;
}

export default RoomModel;
